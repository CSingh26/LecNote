# v1.0.1 implementation

Work occurs only in the isolated `v1.0.1` checkout. The currently running app,
its checkout, dependencies, and library are not modified.

## Six milestones and approved corrective push

1. Complete: class resource storage and explicit lecture preparation (`809cbed`).
2. Complete: full-lecture relevance, review overrides, and efficient OpenAI generation (`02c449a`).
3. Complete: ordered recording merges and safe six-hour audio compression (`2414d6c`).
4. Complete: class resources and preparation interface (`a25ac14`).
5. Complete: relevance review, merge, and storage controls (`8681137`).
6. Complete locally: 300 Python tests, 77 frontend tests, 13 browser tests,
   production build, real-API/UI smoke, documentation, and expanded branch CI.
   GitHub Actions verifies the final pushed commit separately.
7. User-approved corrective push: preserve verified stream duration instead of
   FFmpeg progress timestamps for merge offsets. All 304 Python tests pass locally;
   GitHub Actions reruns the complete workflow after the Linux timing regression.

## Safety and behavior

- Recording finalization transcribes locally without automatically generating notes.
- Generating notes requires a recording note or explicitly selected class resources.
- All transcript content is preserved; uncertain relevance remains reviewable.
- Merging preserves source lectures and absolute source provenance.
- Compression is eligible six hours after finalization, never while recording or
  processing. Replacement follows complete audio validation; failures retain originals.
- Existing custom model settings remain unchanged. New installations use a stronger
  mini-tier OpenAI model with bounded prompts and reusable analysis caches.
- Tests and previews use synthetic data in the isolated checkout.
